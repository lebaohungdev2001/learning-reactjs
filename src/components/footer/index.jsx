import React from 'react';
import PropTypes from 'prop-types';

footer.propTypes = {
    
};

function footer(props) {
    return (
        <div>
            
        </div>
    );
}

export default footer;