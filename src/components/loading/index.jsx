import React from 'react';
import PropTypes from 'prop-types';
import './style.scss'
loading.propTypes = {
    
};

function loading(props) {
    return (
        <div>
            
        </div>
    );
}

export default loading;